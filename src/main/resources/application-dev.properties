# ========================================
# PERFIL DE DESARROLLO (Docker local)
# ========================================

# Base de datos PostgreSQL local (Docker)
spring.datasource.url=jdbc:postgresql://postgres:5432/inmedt_ecommerce
spring.datasource.username=postgres
spring.datasource.password=password
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA - Desarrollo
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# CORS - Desarrollo
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:8085

